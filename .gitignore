.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
build/
dist/

# 测试产物
artifacts/shot_plan_*.json
artifacts/draft_content.json

# IDE
.vscode/
.idea/

# Week 3 runtime state
checkpoints/
drafts/

# Git worktrees (本地并行开发目录，不入库)
.worktrees/

# 本机代理工具目录（gost 二进制 + 私网代理日志，不入库）
.docker-proxy/

# 节点运行产物（job-{verify,xyz,unknown}/ 下的 manifest/artifacts/checkpoints/logs，本机调试用）
outputs/

# 启动脚本生成的 initial-state.json（含本机绝对路径，跨机无意义）
runtime/initial_state_*.json

# 启动日志与输入素材（start_production.ps1 现场产物，跨机无意义）
logs/
inputs/

# OpenStoryline 本地运行时配置（config.toml 含 API key 占位符，本机填入真实 key 后不入库）
openstoryline-config/
