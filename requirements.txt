annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
charset-normalizer==3.5.1
colorama==0.4.6
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
iniconfig==2.3.0
jsonpatch==1.33
jsonpointer==3.1.1
langchain-core==1.6.1
langchain-protocol==0.0.19
# LangGraph 使用公共 PyPI 版本(与本机 venv 锁定的 1.2.9 / 4.1.1 / 1.1.0 一致)
# 如需本地修改版 LangGraph(editable),请额外执行:
#   pip install -r requirements-local-dev.txt
langgraph==1.2.9
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
# Week 5:Postgres checkpointer
langgraph-checkpoint-postgres>=3.1,<4
psycopg[binary,pool]>=3.2
langgraph-sdk==0.4.4
langsmith==0.11.2
orjson==3.12.0
ormsgpack==1.12.2
packaging==26.3
# Phase 5:assembly 视觉观察(plan §9 / 阶段五执行记录 §三)
# visual_observe.py 第 333/543 行 ``import cv2``,第 443 行 ``import numpy as np``。
# 虽然这是函数内 lazy import(ffmpeg 主流程不可用时不会触发),
# 但钉住版本避免新机器装到不兼容 cv2 / numpy(已多次踩坑)。
numpy==2.5.3
opencv-python-headless==4.10.0.84
pillow==12.3.0
pluggy==1.6.0
pydantic==2.13.5
pydantic_core==2.46.5
Pygments==2.21.0
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-mock==3.15.1
PyYAML==6.0.3
requests==2.34.2
requests-toolbelt==1.0.0
# Phase 1:FireRed-OpenStoryline MCP 集成(plan §7.2)
# `mcp` Python SDK 由 `langgraph-sdk==0.4.4` 间接传递,主环境不再主动声明。
# OpenStoryline 现走本地 uvicorn + httpx 健康检查(见 node_02_launch_openstoryline.py)
sniffio==1.3.1
tenacity==9.1.4
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
uuid_utils==0.17.0
websockets==16.1.1
xxhash==4.0.1
zstandard==0.25.0
